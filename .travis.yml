language: ruby
services:
  - mysql
install:
  - bundle install
before_script:
  - mysql.server start
  - bundle exec rake db:create
  - bundle exec rake db:migrate
script: bundle exec rspec spec/models
